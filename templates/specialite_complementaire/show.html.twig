{% extends 'base.html.twig' %}

{% block title %}SpecialiteComplementaire{% endblock %}

{% block body %}
    <h1>SpecialiteComplementaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialite_complementaire.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ specialite_complementaire.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_specialite_complementaire_index') }}">back to list</a>

    <a href="{{ path('app_specialite_complementaire_edit', {'id': specialite_complementaire.id}) }}">edit</a>

    {{ include('specialite_complementaire/_delete_form.html.twig') }}
{% endblock %}
