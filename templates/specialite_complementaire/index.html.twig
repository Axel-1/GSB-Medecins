{% extends 'base.html.twig' %}

{% block title %}SpecialiteComplementaire index{% endblock %}

{% block body %}
    <h1>SpecialiteComplementaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for specialite_complementaire in specialite_complementaires %}
            <tr>
                <td>{{ specialite_complementaire.id }}</td>
                <td>{{ specialite_complementaire.libelle }}</td>
                <td>
                    <a href="{{ path('app_specialite_complementaire_show', {'id': specialite_complementaire.id}) }}">show</a>
                    <a href="{{ path('app_specialite_complementaire_edit', {'id': specialite_complementaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_specialite_complementaire_new') }}">Create new</a>
{% endblock %}
