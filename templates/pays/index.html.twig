{% extends 'base.html.twig' %}

{% block title %}Pays index{% endblock %}

{% block body %}
    <h1>Pays index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pays in paysList %}
            <tr>
                <td>{{ pays.id }}</td>
                <td>{{ pays.nom }}</td>
                <td>
                    <a href="{{ path('pays_show', {'id': pays.id}) }}">show</a>
                    <a href="{{ path('pays_edit', {'id': pays.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('pays_new') }}">Create new</a>
{% endblock %}
