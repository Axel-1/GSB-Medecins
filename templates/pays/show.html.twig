{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pays.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pays.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pays_index') }}">back to list</a>

    <a href="{{ path('pays_edit', {'id': pays.id}) }}">edit</a>

    {{ include('pays/_delete_form.html.twig') }}
{% endblock %}
