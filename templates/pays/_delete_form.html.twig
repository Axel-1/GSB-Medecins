<form method="post" action="{{ path('pays_delete', {'id': pays.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pays.id) }}">
    <button class="btn">Delete</button>
</form>
